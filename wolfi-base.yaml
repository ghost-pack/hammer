contents:
  repositories:
    - https://packages.wolfi.dev/os
  keyring:
    - https://packages.wolfi.dev/os/wolfi-signing.rsa.pub
  packages:
    - wolfi-base
    - go
    - opentofu
    - apko
entrypoint:
  command: /bin/sh -l

archs:
  - x86_64

# use this later to make your base toolkit image.
# Can run locally with docker run --rm apko-wolfi:test-amd64 -c "apko version".